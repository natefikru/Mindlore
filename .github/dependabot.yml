# The workflows are the only dependencies this repo has. One PR a week at most when an action
# moves; nothing else to watch (no SwiftPM, no CocoaPods).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
